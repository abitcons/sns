
interface AnimatedCounterProps {
    end: number;
    duration?: number;
    prefix?: string;
    suffix?: string;
}
export default function AnimatedCounter({ end, duration, prefix, suffix }: AnimatedCounterProps): React.JSX.Element;
export {};
